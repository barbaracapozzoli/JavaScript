// Booleans - True or False (True = 1 and False = 0) Usado normalmente para comparações, conferências dentro do nosso projeto

let ligado = true // (verdadeiro = sim = 1);
let desligado = false // (falso = não = 0);

// console.log(true == 0); - Isso é um teste e um exemplo, vai dar False no terminal porque true == 1
// console.log(false == 1); - Isso é um teste e um exemplo, vai dar False no terminal porque false == 0

let nome = "Paulo";
console.log(Boolean(nome)); // Considerou o nome de Paulo true = 1

let numero = 0;
console.log(Boolean(numero)); // Considerou "0" false, igual a 0
